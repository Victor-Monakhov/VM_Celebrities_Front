import { CelebrityValuePipe } from './celebrity-value.pipe';

describe('CelebrityValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CelebrityValuePipe();
    expect(pipe).toBeTruthy();
  });
});
